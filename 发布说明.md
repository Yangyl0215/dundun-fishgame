# 小鱼碰碰乐发布说明

这是一个纯静态 H5 小游戏，只需要发布下面 3 个文件，就能生成别人也可以打开的公网链接：

- `index.html`
- `styles.css`
- `game.js`

## 最快方式：Netlify Drop

1. 打开 Netlify Drop 页面：https://app.netlify.com/drop
2. 把整个 `G:\Codexproject\fish` 文件夹拖进去。
3. 等待上传完成后，Netlify 会生成一个 `https://...netlify.app` 链接。
4. 把这个链接发给别人，对方用手机、平板或电脑浏览器都可以打开。

## 也可以用 GitHub Pages

1. 新建一个 GitHub 仓库。
2. 上传 `index.html`、`styles.css`、`game.js`。
3. 在仓库设置里开启 Pages，选择主分支和根目录。
4. GitHub 会生成一个 `https://用户名.github.io/仓库名/` 链接。

## 发布前检查

- 首页文件必须叫 `index.html`。
- 3 个文件要放在同一层目录。
- 上传后如果页面能打开但没有样式或游戏，通常是 `styles.css` 或 `game.js` 没有一起上传。
